# This dependency-free example needs no configuration and no API keys — the demo
# and tests run on the standard library alone. The production equivalent lives in
# ../microsoft-agent-framework/, which needs a model endpoint. Never commit .env.
