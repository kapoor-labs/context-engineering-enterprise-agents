# Chapter 9 — context isolation and sub-agents.
#
# Dependency-free: the demo and tests run on the Python standard library alone
# (the sub-agents are deterministic mocks, for offline verification). There is
# nothing to pip install.
#
# A production version would drive the orchestrator and sub-agents with a real
# model, e.g.:
#
#   anthropic>=0.40
#
# (left commented so this folder verifies with a clean, empty environment).
