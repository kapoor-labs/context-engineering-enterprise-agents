# No secrets are required for this example.
# The loader and tests run entirely offline; nothing calls a model API.
# Later chapters' examples introduce endpoint/key variables — each folder
# documents its own in a .env.example like this one.
